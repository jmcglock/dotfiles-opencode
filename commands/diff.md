---
description: Quick git diff summary — staged, unstaged, or specific files
---
Show git diff for: $ARGUMENTS

If no args: show staged changes, then unstaged.
If file args: diff those files.
Summarize in ≤10 lines. Use `file:line` references. Skip formatting noise.
